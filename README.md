# whaledetection
Need python 3.13 because of tensorflow
#DEPENDENCIES
py -3.13 -m venv .venv
.venv\Scripts\activate
python -m pip install --upgrade pip
pip install -e .

Windows ffmpeg:
winget install ffmpeg
restart enviroment after installation